{% extends 'base.html.twig' %}
{% block body %}
    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.firstname) }}
    {{ form_row(form.email) }}
    {{ form_row(form.destination) }}
    {{ form_row(form.destination.country) }}
    {{ form_row(form.destination.location) }}
    {{ form_row(form.destination.arrival) }}
    {{ form_row(form.destination.departure) }}
    <button type="submit" class="btn btn-primary">Envoyer mon form</button>
    {{ form_end(form) }}

{% endblock %}
